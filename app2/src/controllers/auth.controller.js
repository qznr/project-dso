export const register = (req, res) => {
    res.status(200).send({ message: "Auth: Endpoint Registrasi dipanggil. Implementasi segera menyusul." });
};

export const login = (req, res) => {
    res.status(200).send({ message: "Auth: Endpoint Login dipanggil. Implementasi segera menyusul." });
};